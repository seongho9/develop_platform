spring:
  aop: auto=true
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:./db/dbms.db
    hikari:
      maximum-pool-size: 3
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
docker:
  host: 
  tls:
    verify: false
jwt:
  access:
    expire: 600000
    header: Authorization
  refresh:
    expire: 60000000
    header: refresh
  secret: 
host:
  ip: 
  mac: 
  port: 22
  user: seongho
  key: ./key.pem
  #//wsl.localhost/Ubuntu-18.04/home/seongho/keys/home.pem
  #.
  volume: /home/seongho/code-src

